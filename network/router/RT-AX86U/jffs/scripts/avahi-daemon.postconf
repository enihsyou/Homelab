#!/bin/sh
# make mDNS packet across WAN-LAN subnets.
CONFIG=$1

# shellcheck source=/dev/null
. /usr/sbin/helper.sh

pc_delete "deny-interfaces" "$CONFIG"

cat <<EOF >> "$CONFIG"
[reflector]
enable-reflector=yes
EOF
