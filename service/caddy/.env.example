# Cloudflare API Token: requires Zone:DNS:Edit permission (used for both
# DDNS AAAA record updates and Let's Encrypt DNS-01 challenges).
# Create at https://dash.cloudflare.com/profile/api-tokens
# Copy this file to .env and fill in the real token; .env is gitignored.
CLOUDFLARE_API_TOKEN=